apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cpu-hello-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: hello.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cpu-hello-frontend
            port:
              number: 80
